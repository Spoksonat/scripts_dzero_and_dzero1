#### Script para iniciar gamos desde dzero. Se ejecuta desde dzero
#NO OLVIDAR ejecutar: chmod u+x iniciar_gamos_desde_dzero.sh para dar permiso a consola de ejecutar esto

# Ejecucion: source iniciar_gamos_desde_dzero.sh

USER=mf.sanchez17

cd /usr/local/GAMOS/GAMOS.5.0.0/
source config/confgamos.sh 
cd /home/${USER}
